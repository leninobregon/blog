<?php
/**
 * Sitemap XML - Para SEO de Google
 */
require_once 'includes/functions.php';

header('Content-Type: application/xml; charset=utf-8');
$posts = getPosts(100, 0);
$siteUrl = CONFIG['site_url'] ?? 'http://localhost';

echo '<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    <url>
        <loc>' . htmlspecialchars($siteUrl) . '/</loc>
        <changefreq>daily</changefreq>
        <priority>1.0</priority>
    </url>
    <url>
        <loc>' . htmlspecialchars($siteUrl) . '/about.php</loc>
        <changefreq>monthly</changefreq>
        <priority>0.8</priority>
    </url>
';

foreach ($posts as $post) {
    $postUrl = $siteUrl . '/post.php?id=' . $post['id'];
    echo '
    <url>
        <loc>' . htmlspecialchars($postUrl) . '</loc>
        <lastmod>' . date('c', strtotime($post['updated_at'] ?? $post['created_at'])) . '</lastmod>
        <changefreq>monthly</changefreq>
        <priority>0.6</priority>
    </url>';
}

echo '
</urlset>';